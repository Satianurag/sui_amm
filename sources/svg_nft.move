module sui_amm::svg_nft {
    // SVG generation has been removed from on-chain code to avoid
    // excessive gas costs and storage bloat. Rendering is now expected
    // to be done off-chain via the Sui Display standard and external
    // indexers/wallets.
}
